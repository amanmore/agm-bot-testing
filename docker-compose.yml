version: "3.9"
services:
  agm-posts:
    image: agm-posts
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agm-posts
    env_file:
      - .env
    volumes:
      - ./:/app
    restart: unless-stopped
